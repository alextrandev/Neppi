
  # NEPPI

  This is a code bundle for NEPPI. The original project is available at https://www.figma.com/design/Vzn3KxZeyvMuCpzyhFZUJd/NEPPI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  